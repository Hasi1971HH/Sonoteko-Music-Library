name: Sonoteko macOS App bauen

on:
  push:
    tags:
      - 'V*'
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      - name: Python einrichten
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Abhängigkeiten installieren
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: App bauen
        run: |
          pyinstaller --target-arch arm64 Sonoteko.spec

      - name: ZIP erstellen
        run: |
          cd dist
          zip -r "../Sonoteko-macOS.zip" "Sonoteko.app"

      - name: ZIP als Artefakt hochladen
        uses: actions/upload-artifact@v4
        with:
          name: Sonoteko-macOS
          path: Sonoteko-macOS.zip

      - name: Release erstellen (bei Tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: Sonoteko-macOS.zip
          generate_release_notes: true
