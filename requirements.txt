# 6.7.2 is the minimum that ships QT_QPA_NO_BUNDLE_LOOKUP, which prevents the
# CFBundleGetMainBundle() PAC-signature crash on macOS 15+ / 26.x.
PyQt6>=6.7.2
PyQt6-Qt6>=6.7.2

# Audio-Metadaten
mutagen>=1.47.0

# Bild-Verarbeitung (Cover-Art)
Pillow>=10.0.0

# HTTP für Online-Features (MusicBrainz, AcoustID, LRClib)
requests>=2.31.0

# AcoustID-Fingerprinting (benötigt fpcalc/Chromaprint als Binary)
pyacoustid>=1.3.0
